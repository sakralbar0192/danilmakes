export const utils = {
  table_to_sheet: () => ({}),
  book_new: () => ({}),
  book_append_sheet: () => undefined,
};

export function writeFile() {
  console.warn("Excel export is disabled in demo mode");
}
